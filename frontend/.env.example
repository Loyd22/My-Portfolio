# Backend API URL for server-side portfolio fetch
PORTFOLIO_API_BASE_URL=http://localhost:8000

# Optional public URL for client-side API calls (if needed later)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
